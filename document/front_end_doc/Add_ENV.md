In front_end folder, there should be an .env file to store environment variable (hide on github to prevent key leaking)
Create a file name: `.env` 
and add line:
`EXPO_PUBLIC_SERVER_API_URL ="Your NestJS Server URL`